# Placeholder names only. Never commit secrets, tokens, passwords, or production credentials.
APP_ENV=development
DATABASE_URL=CHANGE_ME
REDIS_URL=CHANGE_ME
API_BASE_URL=CHANGE_ME
# Phase 13.01 provisional authentication foundation only. Use a unique secret
# outside source control; no final credential/session policy is selected here.
AUTH_PROVISIONAL_SIGNING_SECRET=CHANGE_ME_MINIMUM_32_CHARACTERS
