import { BadRequestException, ConflictException, Injectable, NotFoundException } from '@nestjs/common';
import { RoutesRepository } from './routes.repository.js';
import { RoutesAuthorizationPolicy, type RoutesAuthorizationContext } from './routes.policy.js';
import { isRouteStatus, isUuid, validateRouteName, type CreateRouteInput, type UpdateRouteInput } from './routes.types.js';

function keys(body: Record<string, unknown>, allowed: readonly string[]): void { if (Object.keys(body).some((key) => !allowed.includes(key))) throw new BadRequestException(); }
function parseCreate(body: unknown): CreateRouteInput { const value = (body ?? {}) as Record<string, unknown>; keys(value, ['school_id', 'name']); if (!isUuid(value.school_id)) throw new BadRequestException(); return { schoolId: value.school_id, name: validateRouteName(value.name) }; }
function parseUpdate(body: unknown): UpdateRouteInput { const value = (body ?? {}) as Record<string, unknown>; keys(value, ['name', 'status', 'version']); if (!Number.isInteger(value.version) || Number(value.version) < 1 || (value.name === undefined && value.status === undefined)) throw new BadRequestException(); if (value.status !== undefined && !isRouteStatus(value.status)) throw new BadRequestException(); return { name: value.name === undefined ? undefined : validateRouteName(value.name), status: value.status as UpdateRouteInput['status'], version: Number(value.version) }; }
function persist<T>(operation: () => Promise<T>): Promise<T> { return operation().catch((e: unknown) => { const code = (e as { code?: string }).code; if (code === 'AUTHORITY_REVOKED' || code === 'ROUTE_VERSION_CONFLICT') throw new ConflictException(); if (code === 'SCHOOL_LIFECYCLE_DENIED' || code === 'TENANT_LIFECYCLE_DENIED' || code === 'ROUTE_LIFECYCLE_DENIED') throw new NotFoundException(); if (code === '23503' || code === '23514') throw new BadRequestException(); if (code === '23505') throw new ConflictException(); throw e; }); }

@Injectable()
export class RoutesService {
  constructor(private readonly repository: RoutesRepository, private readonly policy: RoutesAuthorizationPolicy) {}
  async create(context: RoutesAuthorizationContext, body: unknown) { if (!isUuid(context.principal.subject)) throw new NotFoundException(); const input = parseCreate(body); const school = await this.repository.getSchoolContext(input.schoolId); if (!school || school.status !== 'active' || school.tenantStatus !== 'active' || !this.policy.canManage(context, school.tenantId)) throw new NotFoundException(); return persist(() => this.repository.create(input, { actorId: context.principal.subject, correlationId: context.correlationId, requiredRoles: ['school-admin', 'super-admin'] })); }
  async list(context: RoutesAuthorizationContext, schoolId: string) { if (!isUuid(context.principal.subject) || !isUuid(schoolId)) throw new NotFoundException(); const school = await this.repository.getSchoolContext(schoolId); if (!school || school.status !== 'active' || school.tenantStatus !== 'active' || (!this.policy.isSuperAdmin(context) && school.tenantId !== context.principal.tenantId) || !this.policy.canRead(context, school.tenantId)) throw new NotFoundException(); return this.repository.list(schoolId, this.policy.isSuperAdmin(context) ? undefined : context.principal.tenantId); }
  async get(context: RoutesAuthorizationContext, id: string, schoolId: string) { if (!isUuid(context.principal.subject) || !isUuid(id) || !isUuid(schoolId)) throw new NotFoundException(); const row = await this.repository.get(id, schoolId, this.policy.isSuperAdmin(context) ? undefined : context.principal.tenantId); if (!row || !this.policy.canRead(context, row.tenantId)) throw new NotFoundException(); return row; }
  async update(context: RoutesAuthorizationContext, id: string, schoolId: string, body: unknown) { if (!isUuid(context.principal.subject) || !isUuid(id) || !isUuid(schoolId)) throw new NotFoundException(); const current = await this.repository.get(id, schoolId, this.policy.isSuperAdmin(context) ? undefined : context.principal.tenantId); if (!current || !this.policy.canManage(context, current.tenantId)) throw new NotFoundException(); return persist(async () => { const result = await this.repository.update(id, schoolId, parseUpdate(body), { actorId: context.principal.subject, correlationId: context.correlationId, requiredRoles: ['school-admin', 'super-admin'] }); if (!result) throw Object.assign(new Error('ROUTE_VERSION_CONFLICT'), { code: 'ROUTE_VERSION_CONFLICT' }); return result; }); }
}
