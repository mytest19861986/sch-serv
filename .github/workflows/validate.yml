name: Foundation Validation
on: [pull_request, push]
jobs:
  documentation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate required foundation files
        run: test -f README.md && test -f .env.example && test -d docs
